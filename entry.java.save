package inventory;

import java.util.Scanner;
import java.io.FileReader;
import java.io.BufferedReader;
import item.java;
public class entry{
	public static item[] list=new item[200];
	public static int n=0;

	public static void main(String[] args) {
		String selector, name;
		Scanner stdin=new Scanner(System.in);
		System.out.print("Command: ");
		selector=stdin.next;
		name=stdin.next();
		selector=tolowercase(selector);
		switch(selector)
		{
			case 'e': enter(name);
		}

	}

	public static vodid enter(String name)
	{
		Scanner stdin=new Scanner(System.in);
		int qty;
		String notes;
		System.out.printf("Enter quantity: ");
		qty=stdin.nextInt();
		System.out.printf("Enter notes: ");
		notes=stdin.next();
		if (find(name) != -1) //To be implemented
		{
			
		}
	}

	public static void WriteInventory(String FileName) throws Exception{
		PrintStream P  = new PrintStream(FileName);
							
		for (int i=0; i < num_entries; i++) {
			P.println(entryList[i].name      + "\t" +
					entryList[i].quantity  + "\t" +
					entryList[i].notes);
		}
		P.close();
		System.out.println("Inventory stored.");		
}
	
}
